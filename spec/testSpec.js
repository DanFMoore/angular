describe('Filter', function() {
    it('readableEvent should return a readable label for the given event', function() {
        var result = 'Inactive';
        expect(result).toEqual('Inactive');
    });
});
