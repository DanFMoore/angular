'use strict';

/**
 * This file is the entry point for webpack. All files required here will be bundled up into one file for the browser.
 * Settings are defined in webpack.config.js
 */

require('./filters');
require('./app');
require('./ajax');
require('./routes');